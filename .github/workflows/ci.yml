name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Build, Check, Test
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build workspace
        run: pnpm build

      - name: Check publish packages
        run: |
          pnpm --filter @drift-lock/core pack --dry-run
          pnpm --filter @drift-lock/cli pack --dry-run
          pnpm --filter @drift-lock/eslint-plugin pack --dry-run

      - name: Check DriftLock CLI binary
        run: pnpm --filter next-v1 exec drift-lock --help

      - name: Typecheck workspace
        run: pnpm check

      - name: Test workspace
        run: pnpm test

      - name: Check root DriftLock lint
        run: pnpm lint

      - name: Check Next demo lint
        run: pnpm --filter next-v1 lint

      - name: Check Next demo DriftLock contracts
        run: pnpm --filter next-v1 drift-lock:check

      - name: Verify generated DriftLock index is current
        run: |
          pnpm --filter next-v1 drift-lock:extract
          if [ -n "$(git status --porcelain -- apps/next-v1/.drift/contracts.generated.index)" ]; then
            git status --short -- apps/next-v1/.drift/contracts.generated.index
            exit 1
          fi

      - name: Check root DriftLock contracts
        run: pnpm drift-lock:check

      - name: Report root DriftLock coverage
        run: pnpm drift-lock:coverage

      - name: Verify root DriftLock index is current
        run: |
          pnpm drift-lock:extract
          if [ -n "$(git status --porcelain -- .drift/contracts.generated.index)" ]; then
            git status --short -- .drift/contracts.generated.index
            exit 1
          fi
