# drift-lock

CLI for DriftLock.

```bash
npx drift-lock install
drift-lock check
drift-lock context <file>
```

DriftLock extracts local `@drift` contracts, renders agent-ready context, and
checks supported invariants in CI.
