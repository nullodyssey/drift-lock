# @drift-lock/core

Core parser, extractor, context renderer, and deterministic checks for DriftLock.

This package is intended for the DriftLock CLI and ESLint plugin. Most users
should install `drift-lock` instead.
