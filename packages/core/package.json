{
  "name": "@drift/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean --out-dir dist",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "typescript": "^5.9.3",
    "yaml": "^2.8.1"
  }
}
